{
  "name": "swimlogs-api",
  "version": "1.0.0",
  "scripts": {
    "preview": "bunx @redocly/cli preview-docs api.yaml",
    "bundle": "bunx @redocly/cli bundle -o swimlogsAPI.gen.yaml api.yaml",
    "lint": "bunx @redocly/cli lint api.yaml",
    "clean-generate": "bun run clean && bun run generate-go &&  bun run generate-ts",
    "generate": "bun run generate-go && bun run generate-ts",
    "generate-go": "bun run bundle && oapi-codegen -generate types -o api.gen.go -package apidef swimlogsAPI.gen.yaml",
    "generate-ts": "bun run bundle && ./generate-ts-openapi.sh",
    "clean": "bun run clean-go && bun run clean-ts",
    "clean-go": "rm api.gen.go",
    "clean-ts": "rm -rf ./runtime.ts ./index.ts ./models ./apis"
  },
  "license": "MIT",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.13.4"
  }
}
