description: Request for moving set in order of training sets
required: true
content:
  application/json:
    schema:
      type: object
      properties:
        newSetOrder:
          type: integer
          description: New setOrder of the set
          minimum: 0
          maximum: 49
      required:
        - newSetOrder
