description: Request for creating a training
required: true
content:
  application/json:
    schema:
      $ref: "../schemas/NewTraining.yaml"
      required:
        - start
        - durationMin
        - totalDistance
        - sets
