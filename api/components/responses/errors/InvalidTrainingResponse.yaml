description: Contains reason why training is invalid
content:
  application/problem+json:
    schema:
      oneOf:
        - $ref: "../../schemas/errors/InvalidSessionError.yaml"
        - $ref: "../../schemas/errors/InvalidSetError.yaml"
