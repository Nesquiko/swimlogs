description: Training successfully edited
content:
  application/json:
    schema:
      $ref: "../schemas/TrainingSummary.yaml"
      required:
        - id
        - start
        - durationMin
        - totalDistance
