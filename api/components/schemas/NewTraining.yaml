allOf:
  - $ref: "./BaseTraining.yaml"
  - type: object
    properties:
      sets:
        minItems: 1
        maxItems: 50
        type: array
        items:
          $ref: "./NewTrainingSet.yaml"
    required:
      - start
      - durationMinutes
      - sets
